name: CI

on:
  push:
    branches: [master, development]
  pull_request:
    branches: [master, development]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Vet
        run: CGO_ENABLED=0 go vet -tags containers_image_openpgp ./...

      - name: Test
        run: CGO_ENABLED=0 go test -tags containers_image_openpgp ./...

      - name: Build
        run: CGO_ENABLED=0 go build -tags containers_image_openpgp ./...
